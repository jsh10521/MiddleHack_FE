// 날씨 정보 API 서비스

export const weatherService = {
    // 현재 날씨 조회
    getCurrentWeather: async () => {
        // API 호출 로직
    },

    // 날씨 예보 조회
    getWeatherForecast: async () => {
        // API 호출 로직
    },

    // 기상 경보 조회
    getWeatherAlerts: async () => {
        // API 호출 로직
    },
};
